$CREATEREPORT($IF(&ARG1)&ARG1$ELSE$CIRCUITNAME\.net$END)
$INCLUDEPORTSON
$BUSNAMEON(_)
$FIND $INTERFACE(&ARG2)
$ITEMSEPARATOR(,)
$LINETERMINATOR()
$SETVAR(COMMA,)
define $IF($TYPENAME)$TYPENAME$ELSE$CIRCUITNAME$END\(
$DEVICES&COMMA$SETVAR(COMMA,\,)$PINS
)
$INCLUDEPORTSOFF
$LINETERMINATOR $WINDOWS

$FIND $DEVICES
$DEVPINFORMAT $SIGNAME
\{
$ITEMSEPARATOR(,)
$DEVICES$IF(&Part)&Part$ELSE$TYPENAME$END$TAB$DEVNAME\($PINS)
\}